.max-w-3xl.mx-auto
  %h1.text-3xl.font-bold.text-brown-black.mb-8 Edit Order ##{@order.id}
  = link_to "← Back to Order", admin_order_path(@order), class: "text-amber-600 hover:text-amber-700 font-medium mb-6 inline-block"

  .bg-white.rounded-lg.shadow-lg.p-8
    = form_with model: [:admin, @order], local: true, class: "space-y-6" do |form|
      - if @order.errors.any?
        .bg-red-100.border.border-red-400.text-red-700.px-4.py-3.rounded.mb-4
          %h3.text-sm.font-medium.mb-2
            = pluralize(@order.errors.count, "error") %> prohibited this order from being saved:
          %ul.list-disc.pl-5
            - @order.errors.full_messages.each do |message|
              %li= message

      %div
        = form.label :user_id, "Customer", class: "block text-sm font-medium text-gray-700 mb-2"
        = form.select :user_id, options_from_collection_for_select(@users, :id, :email, @order.user_id), {}, { class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent" }

      %div
        = form.label :total, class: "block text-sm font-medium text-gray-700 mb-2"
        = form.number_field :total, step: 0.01, value: @order.total, class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent"

      %div
        = form.label :status, class: "block text-sm font-medium text-gray-700 mb-2"
        = form.select :status, options_for_select([["Pending", "pending"], ["Paid", "paid"], ["Shipped", "shipped"], ["Delivered", "delivered"], ["Cancelled", "cancelled"]], @order.status), {}, { class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent" }

      .flex.gap-4
        = form.submit "Update Order", class: "bg-amber-600 text-white px-6 py-2 rounded-lg hover:bg-amber-700 transition-colors"
        = link_to "Cancel", admin_order_path(@order), class: "bg-gray-300 text-gray-700 px-6 py-2 rounded-lg hover:bg-gray-400 transition-colors"









