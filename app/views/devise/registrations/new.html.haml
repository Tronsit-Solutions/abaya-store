.auth-container.min-h-screen.flex.items-center.justify-center.px-4.py-12.bg-gradient-to-br{:style => "background: linear-gradient(135deg, hsl(var(--color-bg)) 0%, hsl(var(--color-surface-2)) 100%);", data: { controller: "reveal" }}
  .auth-card.max-w-md.w-full.card.p-8.reveal-initial{ data: { reveal_target: "item" } }
    / Header
    .text-center.mb-8
      .mb-4
        %h1.text-4xl.font-bold.text-brown-black.mb-2 Create Account
        .h-1.w-20.bg-amber-600.mx-auto.rounded
      %p.text-gray-600 Join Das Club and start your elegant journey
    / Form
    = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "space-y-6" }) do |f|
      = render "devise/shared/error_messages", resource: resource
      .space-y-5
        / Email Field
        .field
          = f.label :email, class: "block text-sm font-medium text-brown-black mb-2"
          .relative
            = f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all bg-white", placeholder: "your@email.com"
            
        / Password Field
        .field
          = f.label :password, class: "block text-sm font-medium text-brown-black mb-2"
          - if @minimum_password_length
            %span.text-xs.text-gray-500.ml-1 (#{@minimum_password_length} characters minimum)
          .relative
            = f.password_field :password, autocomplete: "new-password", class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all bg-white", placeholder: "••••••••"
            
        / Password Confirmation Field
        .field
          = f.label :password_confirmation, class: "block text-sm font-medium text-brown-black mb-2"
          .relative
            = f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-amber-500 focus:border-transparent transition-all bg-white", placeholder: "Confirm password"

      / Submit Button
      .actions.pt-2
        = f.submit "Create Account", class: "w-full bg-black text-white py-3 rounded-lg font-semibold hover:bg-gray-800 transition-all duration-300 transform hover:scale-[1.02] shadow-lg hover:shadow-xl"
    / Sign In Link
    .mt-6.text-center.pt-6.border-t.border-gray-200
      %p.text-gray-600.text-sm
        Already have an account?
        = link_to "Sign in", new_session_path(resource_name), class: "text-amber-600 hover:text-amber-700 font-semibold transition-colors"

